{
  "name": "workspace",
  "version": "1.0.0",
  "description": "This directory is a brief example of a [Hugo](https://gohugo.io/) app that can be deployed to Vercel with zero configuration.",
  "main": "index.js",
  "scripts": {
   "dev": "npx tinacms dev -c && hugo server",
    "build": "npx tinacms build && hugo",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tinacms": "^2.3.0"
  },
  "devDependencies": {
    "@tinacms/cli": "^1.6.10"
  }
}
